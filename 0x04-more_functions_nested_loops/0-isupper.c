#include "main.h"
#include <ctype.h>
/**
 * _isupper - entry
 * @c: the character
 * Description - The function check if uppercase
 * Return: 1 if true 0 if othewise
 */
int _isupper(int c)
{
if (isupper(c))
return (1);
else
return (0);
}
