#include "main.h"
#include <stdio.h>
/**
 * mul - entry
 * @a: first operand
 * @b: second oprand
 * Description - multiplies two number
 * Return: Always return result
 */
int mul(int a, int b)
{
return (a * b);
}
