#include <stdio.h>
/**
 * main - entry point
 * Description - This function prints all digit of base 10
 * Return: Always 0
 */
int main(void)
{
	int i;
for (i = 0; i < 10; i++)
printf("%d", i);
putchar(10);
return (0);
}
